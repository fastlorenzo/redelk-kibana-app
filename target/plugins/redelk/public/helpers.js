"use strict";
// import { IOCDoc } from './types';
// export const getByPath = (path: string, obj: IOCDoc, separator='.') => {
//     let properties: string[] = Array.isArray(path) ? path : path.split(separator)
//     try {
//       const res = properties.reduce((prev, curr) => prev && typeof curr !== 'string' ? prev[curr] : curr, obj);
//       return res !== undefined ? res : null;
//     } catch (error) {
//       return null;
//     }
// }
